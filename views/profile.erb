<!DOCTYPE html>
<head>
    <link href='https://fonts.googleapis.com/css?family=Lato:400,700,300italic' rel='stylesheet' type='text/css'>
    <link rel="icon" href="//d2c87l0yth4zbw-2.global.ssl.fastly.net/i/_global/favicon.png">
    <link rel="stylesheet" href="app.css" />
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
  <title>Account Overview - Spotifyifyly</title>
</head>

<body class="page-account-overview">

    <div class="page-header">
      <h1 class="account">Account Overview</h1>
    </div>

    <div class="col-md-6">
        <div class="profile">
            <h3 class="text-primary">Profile</h3>
            <form role="form">
                <div class="form-group">
                    <label class="control-label">Email</label>
                    <p class="form-control-static"><%= "#{current_user.email}" %></p>
                    <label class="control-label">Name</label>
                    <p class="form-control-static"><%= "#{current_user.name}" %></p>
        </div>
      </div>
      <div class="col-md-6">
        <h2 class=songs>User Songs</h2>

        <ul>
            <% @user_songs.each do |song| %>
                <li>
                    <strong><%= song.artist %></strong>: <%= song.title %>
                </li>
            <% end %>
        </ul>
        </div>
      </div>
</body>
